/**
 * VIN Intelligence hooks barrel.
 * NOTE: useVinManagement / usePartsExtraction were deleted — both were
 * fully dead code (duplicating live logic inside VinsTab /
 * PartsExtractTab). Verified unused across the codebase during audit.
 */
export { useVinIntelligence } from './useVinIntelligence';
