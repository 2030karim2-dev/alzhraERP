const fs = require('fs'); const env = fs.readFileSync('.env', 'utf8'); const key = env.match(/VITE_GEMINI_API_KEY=(.*)/)[1].trim(); const { GoogleGenAI } = require('@google/genai'); const ai = new GoogleGenAI({ apiKey: key }); ai.models.list().then(res => { for(let i=0; res && i < res.length; i++) { if(res[i].name.includes('flash')) console.log(res[i].name); } }).catch(console.error);
