# Phase 3 TODO — Commission Engine

- [x] إضافة أنواع ومساعدات Supabase لجداول `incentive_*`.
- [x] إضافة استدعاء آمن لـ`incentive_calculate_period` مع تحقق الصلاحيات وحالات الفترات التجريبية.
- [x] بناء لوحة العمولات التحليلية العربية RTL.
- [x] بناء إدارة الخطط والقواعد والشرائح بصلاحيات finance_manager/admin/owner.
- [ ] بناء دورة حياة الفترات مع منع lock/pay/post لفترات الاختبار.
- [x] بناء قائمة الفواتير المعلقة وتوزيع المهندسين والتحقق الذري من 100%.
- [ ] بناء صفحات الحسابات والتفاصيل والتدقيق والدفعات والتعديلات.
- [ ] إضافة تقارير قابلة للطباعة والتصدير مع عزل الشركة والفرع والمهندس.
- [x] إضافة تشغيل دوري آمن بعد قراءة إرشادات periodic updates وتحديد آلية retries/idempotency.
- [x] كتابة Vitest tests لمحرك الحساب والصلاحيات والعزل والحالات التجريبية.
- [ ] تشغيل type-check وbuild والتحقق البصري التفاعلي؛ Vitest وlint المركّز وRTL الساكن تم التحقق منها.
- [ ] حفظ checkpoint/تسليم Phase 3 بعد مراجعة diff المستودع الأب؛ checkpoint WebDev للمشروع النشط جاهز بعد اعتماد المستخدم، بينما type-check/build الكاملان غير معتمدين بسبب ضغط الذاكرة.

## Completed

- [x] بدء Phase 3 بعد موافقة المستخدم الصريحة.
- [x] إضافة دالة PostgreSQL ذرية `public.incentive_calculate_period(company_id, period_id)` إلى مشروع Supabase، مع transaction rollback، حسابات per-user، calculation lines، وحماية period guard.

## ملاحظات تنفيذية مضافة

- [x] قراءة إرشادات التشغيل الدوري: يمنع `setInterval` و`node-cron`، ويجب استخدام Heartbeat عبر `/api/scheduled/*` مع idempotency ومهلة دقيقتين.
- [x] إضافة لوحة أولية عربية RTL ومسار `/commissions` وطبقة استعلامات Supabase ومحرك حساب PostgreSQL.
- [x] تسجيل تشغيل دوري فعلي في Supabase alzhra100 عبر pg_cron، مع دالة نظامية idempotent لا تعتمد على auth.uid().
- [ ] إعادة فحص TypeScript وVite build بعد تخفيف ضغط الذاكرة؛ المحاولة المرحلية الأخيرة انتهت بالمهلة 124 دون أخطاء تشخيصية، مع بقاء ضغط الذاكرة مرتفعاً.

## نتائج التحقق المرحلية

- [x] اختبارات `engineGuards.test.ts`: نجحت 5 اختبارات.
- [ ] ESLint الكامل: لم يُعتمد؛ المستودع يملك 204 مخالفة قائمة، منها مخالفات قديمة في `MainLayout`، ويحتاج تنظيفًا منفصلًا.
- [ ] TypeScript: أوقف بعد 30 ثانية لتجاوز العملية المهلة/الذاكرة، ولا توجد نتيجة نجاح معتمدة بعد.
- [ ] Vite build: أوقفته البيئة بالرمز 143 أثناء معالجة dependency كبيرة؛ يجب إعادة المحاولة في بيئة أخف أو عبر build مجزأ.
- [x] التشغيل الدوري: تم اعتماد backend Supabase alzhra100 الموجود فعلياً؛ Edge Functions الحالية لا تخص العمولات، لذلك أُنشئت دالة PostgreSQL نظامية وجدولة pg_cron بدلاً من إضافة endpoint وهمي إلى Vite static.

## نتيجة ESLint المركّز

- [ ] فحص ESLint للمسارات والـMainLayout لم ينجح: ظهرت 167 مخالفة، معظمها قواعد قائمة في `MainLayout` والصفحات الجديدة مضغوطة في أسطر طويلة؛ لا يُعد ذلك فشلًا وظيفيًا، لكنه يتطلب refactor مرحلي قبل التسليم النهائي.
- [ ] إعادة تنسيق `CommissionPeriodsPage` إلى مكونات أصغر ومعالجة مخالفات الأمن/الأنواع بدل تعطيل القواعد.

- [x] تحديث عقد `src/core/database.types.ts` ليتطابق مع توقيعات RPC الفعلية في alzhra100، بما في ذلك create_rule ووسائط engineer_link وperiod_transition.
- [x] إزالة `any` من API ولوحتي العمولات والإعدادات، وإضافة `createCommissionRule` عبر RPC الإداري.

- [ ] Build النهائي ما زال غير معتمد: Vite أنهى العملية بالرمز 143 أثناء تحويل dependency كبيرة حتى مع `NODE_OPTIONS=--max-old-space-size=3072`؛ يجب تشغيله في بيئة أخف أو تقسيم التحقق قبل checkpoint.
- [x] إضافة صفحة تقارير الفترة مع CSV والطباعة، وربطها بالتوجيه والقائمة الجانبية.

- [x] إضافة `authorization.ts` لعزل الشركة والفرع والمهندس وصلاحيات إدارة الخطط وانتقالات الفترة.
- [x] اختبارات `engineGuards.test.ts` و`authorization.test.ts`: نجحت 9 اختبارات.

- [x] إصلاح إنشاء الخطط: أصبح يمر عبر `incentive_create_plan` مع قراءة لاحقة معزولة بالشركة، بدل INSERT مباشر يتجاوز عقد الصلاحيات والتدقيق.
- [ ] تطبيق نفس المبدأ على CRUD الشرائح والتعديلات؛ أُضيف مسار إنشاء القواعد عبر `incentive_create_rule`، بينما لا توجد بعد واجهة كتابة للشرائح في الصفحة الحالية.

- [x] إعادة تشغيل الاختبارات بعد تعديل API: نجحت 9 اختبارات دون تراجع.

- [ ] مراجعة تغييرات إعدادات الجذر الناتجة عن تثبيت ESLint/PNPM (`package.json`, lockfiles، إعدادات Vite/Tailwind/TypeScript) وفصل الضروري منها عن Phase 3 قبل checkpoint.
- [ ] مراجعة diff النهائي على مستوى المستودع الأب؛ أمر git أظهر ملفات إعدادات خارج مجلد التطبيق بالإضافة إلى ملفات الوحدة الجديدة.

- [x] تحويل إنشاء خطة العمولة إلى `incentive_create_plan`.
- [x] تحويل تحديث خطة العمولة إلى `incentive_update_plan`.
- [x] إعادة تشغيل اختبارات العمولات بعد التعديل: 9/9 ناجحة.

- [x] إضافة تعريفات incentive_* وRPCs إلى `database.types.ts`.
- [x] إزالة `any` من API ولوحتي العمولات والإعدادات.
- [x] تشغيل اختبارات الوحدة بعد التعديل: 9/9 ناجحة.
- [ ] استكمال مخالفات lint اليدوية في المكونات الكبيرة؛ الإصلاح الآلي أنجز الجزء القابل للإصلاح، والمتبقي يتضمن تفكيكاً بنيوياً.
- [ ] تشغيل فحص TypeScript كامل في بيئة بذاكرة أعلى؛ العملية الحالية أُوقفت SIGTERM بسبب ضغط الذاكرة.

- [x] التحقق من مشروع Supabase/backend المرتبط بالاسم `alzhra100` عبر اتصال MCP.
- [x] تحديد ما إذا كان backend يحتوي endpoint أو Edge Function صالحاً للتشغيل الدوري؛ لم توجد Edge Function للعمولات، وpg_cron مفعّل.
- [x] ربط كشف الفواتير المعلقة بنقطة تشغيل آمنة عبر `incentive_detect_pending_invoices_system()` وجدولة `incentive-detect-pending-invoices` كل 15 دقيقة في alzhra100.

- [x] مراجعة CRUD القواعد والشرائح والتعديلات: تم تحديد RPCs الفعلية، وإضافة إنشاء القواعد؛ لا يوجد RPC شرائح/تعديلات مستخدم في الصفحة الحالية.
- [x] التحقق من عدم وجود INSERT/UPDATE/DELETE مباشر داخل وحدة العمولات؛ عمليات الكتابة الحالية تمر عبر RPCs أو لم تُنفذ بعد.
- [x] تفكيك `CommissionPeriodsPage` إلى قائمة وتحكم وcontroller hook، وإغلاق lint للمسارات الأربعة بنجاح.
- [x] إعادة تشغيل Vitest: 9/9 ناجحة، وESLint لـ`api.ts` وصفحة الفترات ومكوّناتها: ناجح؛ ما زال type-check/build الكاملان مؤجلين بسبب ضغط الذاكرة.
- [x] مراجعة RTL والاستجابة في صفحة الفترات: `dir=rtl`، تخطيط mobile-first، و`lg` فقط للتقسيم الجانبي؛ يلزم تحقق بصري تفاعلي عند توفر خادم المستودع الرئيسي.

- [x] مراجعة تغطية تقارير العمولات للتصدير والطباعة وعزل الشركة والصلاحيات؛ أضيف اختيار الفترة مع استعلامات مقيدة بالشركة، وتصدير CSV وطباعة RTL.
- [x] مراجعة انتقالات الفترات وحماية Test Periods مقابل حالات RPC الفعلية؛ الصفحة تمنع القفل والدفع تجريبياً والـRPC يفرض الحماية خادمياً.
- [x] تنفيذ تحقق منخفض الذاكرة متسلسل؛ Vitest نجح 9/9، وlint نجح للـAPI والفترات والتقارير، بينما بقيت مخالفات في Dashboard/Configuration وengineGuards القديمة.
- [x] تحديث توثيق Phase 3 بنتائج التحقق المرحلية والقيود المتبقية في `todo.md`؛ البناء الكامل والفحص البصري التفاعلي ما زالا غير معتمدين.

- [x] فحص مخالفات ESLint الحالية في Dashboard وConfiguration وengineGuards وتحديد الإصلاحات الآمنة.
- [x] إصلاح مخالفات engineGuards المتعلقة بالشروط nullable، وإعادة هيكلة Configuration وDashboard إلى hooks ومكونات typed؛ lint للمسارات الثلاثة ناجح.
- [x] إعادة تشغيل Vitest وESLint المركّز بعد الإصلاحات: ESLint النهائي لـAPI والأنواع والمنطق ناجح، وVitest 9/9 ناجحة.
- [x] محاولة تحقق TypeScript مجزأ بحد 1024MB؛ الفحص العام لم يكتمل بسبب الذاكرة، لكن فحص API والمنطق بحد 512MB نجح مع `vite/client`.
- [x] `git diff --check` ناجح على ملفات وحدة العمولات والأنواع والتوثيق.

- [x] تجربة TypeScript على نطاق ملفات API والمنطق؛ نجح الفحص بعد تضمين `vite/client` ومعالجة حالات enum وJSON، دون أخطاء داخل ملفات العمولات.
- [x] تجربة Vite/build بعد تنظيف العمليات الثقيلة؛ فشل عند `transforming` بسبب `JavaScript heap out of memory` مع حد 768MB، وتم توثيق النتيجة دون اعتماد البناء.
- [x] تحديث توثيق الاعتماد النهائي في `todo.md` بما يفرق بين فحص API/المنطق الناجح وفشل TypeScript/Vite الكامل بسبب حدود الذاكرة.

- [x] محاولة إكمال Vite build ببدائل منخفضة الذاكرة؛ استمر فشل البيئة بسبب JavaScript heap out of memory/SIGTERM، وتم توثيق ذلك دون اعتبار التطبيق فاشلاً.
- [x] تدقيق RLS وRPCs وصلاحيات الأدوار وعزل الشركة والفرع، بما في ذلك مسارات الخدمة والجدولة؛ تم تصحيح search_path لدوال SECURITY DEFINER وتقييد EXECUTE.
- [x] تدقيق API والواجهات بحثاً عن التناقضات والتكرارات والكتابات المباشرة والثغرات النوعية؛ لا توجد كتابات مباشرة في وحدة العمولات، وESLint المصدر يمر بنجاح.
- [x] تدقيق حالات دورة الحياة، Test Periods، التعيينات، التقارير، والتصدير والطباعة؛ الحماية الخادمية للفترات التجريبية ومسارات التقارير والتصدير موثقة ومطبقة.
- [x] تدقيق RTL والاستجابة والحالات الفارغة والأخطاء وإمكانية الاستخدام في صفحات العمولات؛ التحقق البصري التفاعلي الكامل على خادم المستودع الأب يبقى خارجياً بسبب اختلاف المشروع النشط.
- [x] إصلاح العيب الأمني المكتشف في search_path وEXECUTE العام، ثم إعادة فحص ACL واختبار دالة الكشف الدوري بنجاح؛ ما زالت جولات التدقيق الوظيفي والواجهة قيد المتابعة.

## جولة التحسين والتدقيق الإضافية

- [x] تدقيق شامل لتكرار الدوال والأنواع والـhooks والمكونات داخل وحدة العمولات؛ تم توحيد parser وعقد تحديث الخطة وإزالة النوع المكرر.
- [x] تدقيق اتساق حالات الواجهة والتحميل والخطأ والفراغ والصلاحيات في جميع صفحات العمولات؛ تم أيضاً تعريب حالات تقرير العمولات في العرض وCSV.
- [x] تدقيق شامل لعزل الشركة والفرع والمهندس في API وRPCs وسياسات RLS والجدولة؛ أضيفت سياسات RLS تقييدية وتصحيحات RPC والتزامن.
- [x] تدقيق التناقضات بين database.types.ts وعقود Supabase الفعلية واستدعاءات API؛ تم تضييق عقد updateCommissionPlan وتوحيد parsing.
- [x] تدقيق RTL والاستجابة للشاشات الصغيرة ومسارات التركيز وإمكانية الوصول؛ التحقق البصري التفاعلي على خادم المستودع الأب يبقى قيداً خارجياً.
- [x] إضافة تحقق regression وإعادة تشغيل Vitest وESLint والفحوصات المجزأة؛ Vitest 9/9 وESLint للمسارات المعدلة ناجحان، مع مخالفات بنيوية قائمة في auth/store خارج نطاق وحدة العمولات.
- [x] تحديث تقرير commission-deep-audit.md بنتائج الجولة الجديدة والقيود المتبقية.
- [x] مراجعة diff النهائي وتحديد الملفات الآمنة للـcheckpoint دون حفظ تغييرات غير مرتبطة؛ `git diff --check` ناجح، لكن checkpoint مؤجل لأن مشروع WebDev النشط لا يطابق مسار المستودع الأب.

## جولة إعادة التنظيم والتنظيف

- [x] حصر الملفات والمكونات والـhooks الكبيرة وتحديد التكرار ونقاط الفصل الآمنة؛ تم فصل لوحة التحكم والتقارير وطبقة parsers ومفاتيح الاستعلام.
- [x] تقسيم API والأنواع والمساعدات المشتركة إلى ملفات واضحة دون تغيير العقود العامة؛ نُقلت parsers والثوابت إلى `api/parsers.ts` وأضيفت وحدات labels وquery keys المشتركة.
- [x] تقسيم صفحات العمولات ومكوناتها إلى وحدات صغيرة قابلة لإعادة الاستخدام؛ فُصلت widgets لوحة التحكم، وملخص/جدول التقارير، ومساعد CSV.
- [x] تنظيف التسمية والتنسيق وإزالة التكرار مع الحفاظ على RTL والصلاحيات؛ تم توحيد labels وquery keys وفصل منطق العرض والتصدير مع بقاء الإنتاج RTL.
- [x] إعادة تشغيل Vitest وESLint وTypeScript المجزأ وتسجيل النتائج؛ ESLint لملفات الإنتاج وTypeScript للمسارات المعدلة ناجحان، وVitest النهائي 9/9.
- [x] تحديث تقرير التدقيق بوصف refactoring والقيود والملفات المتغيرة.

## جولة الاستمرار والتنظيم الثانية

- [x] حصر الملفات المتبقية الكبيرة وتحديد أولويات فصل الفترات والإعدادات والتعيينات؛ نُفذت الأولوية على التعيينات والإعدادات.
- [x] فصل مكونات الفترات والتحكم بالانتقالات والـhooks دون تغيير السلوك؛ مكونات الفترات كانت مفصولة مسبقاً، وتم تعزيز نمط controller في التعيينات.
- [x] تنظيف صفحات الإعدادات والتعيينات واستخراج الوحدات المشتركة؛ فُصلت مكونات التعيينات وcontroller ومكونات الإعدادات وأعيد استخدام query keys المشتركة.
- [x] مراجعة إمكانية الوصول والتسمية والتنسيق وعدم تكرار الاستدعاءات؛ تمت مراجعة أزرار الوحدة، وتعريب طرق الحساب، وإزالة مفاتيح الاستعلام المحلية المتكررة.
- [x] تشغيل Vitest وESLint وTypeScript المجزأ بعد الجولة الجديدة؛ اختبارات العمولات 9/9 وESLint الإنتاج وTypeScript المجزأ ناجحة، بينما فشل Vitest العام في اختبارين قديمين داخل dhikr خارج النطاق.
- [x] تحديث تقرير التدقيق بنتائج الجولة الجديدة والقيود المتبقية، بما في ذلك فشل dhikr العام المنفصل.

## خطة تحسين واجهة ERP — Mobile & Desktop Responsive UI

- [ ] تدقيق الوضع الحالي وجرد الصفحات والمكونات ومسارات الفواتير على الهاتف واللوحي وسطح المكتب.
- [ ] اعتماد نظام قياسات وكثافة واستجابة موحد يحافظ على الهوية البصرية وRTL.
- [x] تحسين الهيدر والتنقل وأشرطة الإجراءات للهاتف فقط؛ تم ضغط MicroHeader والتنقل السفلي ونافذة المزيد مع الحفاظ على أزرار اللمس.
- [ ] إعادة تنظيم نماذج الفواتير وجداول الأصناف للإدخال السريع دون تغيير منطق الأعمال.
- [ ] تطبيق تحسينات الكثافة على المحاسبة والمخزون والتقارير وبقية الصفحات ذات الصلة.
- [ ] اختبار المقاسات المحددة والاستجابة وإمكانية الوصول وعدم الانحدار الوظيفي.
- [ ] توثيق النتائج والقيود وتجهيز نسخة قابلة للمراجعة والتسليم.

## نطاق الهاتف المحمول فقط

- [ ] حصر الصفحات والمكونات التي تحتاج تحسيناً في breakpoint الهاتف فقط.
- [x] تطبيق تغييرات mobile-only على المقاسات والمسافات والتخطيطات دون تعديل قواعد سطح المكتب أو اللوحي.
- [ ] تحسين نماذج الفواتير والجداول والتنقل والإجراءات للشاشات الصغيرة فقط.
- [ ] التحقق من عدم تأثر سطح المكتب واللوحي والوظائف والصلاحيات.
- [ ] تشغيل اختبارات regression وESLint وTypeScript وتوثيق نتائج تحسين الهاتف.

## Mobile-only continuation — current pass
- [x] فصل جدول المشتريات إلى header/body/toolbar وhook مستقل للتنقل السريع بين الخلايا، مع الحفاظ على منطق البحث والإضافة والحذف.
- [x] إزالة مخالفات ESLint الخاصة بملفات جدول المشتريات الجديدة والمعدلة؛ الفحص المركّز ناجح.
- [x] ضغط مرشح التاريخ وحاوية صفحة المحاسبة على الهاتف فقط.
- [x] ضغط رأس صفحة المخزون وأزرار العرض وحاويات التفاصيل على الهاتف فقط.
- [x] ضغط تصنيفات التقارير وحاوية المحتوى على الهاتف فقط.
- [x] تشغيل الاختبارات المجزأة وتحديث خطة التنفيذ؛ التحقق المرئي النهائي مؤجل بسبب اختلاف مسار المستودع عن مشروع WebDev النشط.

## Purchase invoice mobile density pass
- [x] ضغط جميع مكونات صفحة فاتورة المشتريات على الهاتف فقط إلى كثافة تقارب النصف، بما يشمل الرأس والتبويبات والحقول وجدول الأصناف والإجراءات، دون تغيير سطح المكتب أو اللوحي.
- [x] التحقق من عدم فقدان قابلية اللمس والقراءة أو منطق الإدخال بعد ضغط صفحة فاتورة المشتريات؛ اختبارات مخزن المشتريات 14/14 وdiff check ناجح.

## Purchase invoice density refinement
- [x] استكمال تقريب وضغط تبويبات فاتورة المشتريات، منطقة الجدول، صفوف الأصناف، الإجماليات، وأزرار الإجراءات على الهاتف فقط.
- [x] إعادة التحقق من المساحات الفارغة، التمرير الأفقي، وأهداف اللمس بعد جولة الضغط الثانية؛ Vitest 14/14 وESLint المركز وdiff check ناجحة.

- [x] رفع جميع تحديثات alzhraERP المحلية إلى مستودع GitHub `2030karim2-dev/alzhraERP` والتحقق من نجاح الدفع.

- [ ] إجراء تدقيق عميق للواجهة الأمامية وSupabase backend يشمل الأخطاء والعيوب والتكرار والثغرات والتناقضات وتناسق التكامل بين العقود والاستعلامات والصلاحيات.
- [ ] إعداد تقرير تدقيق موثق بدرجات الخطورة وخطة إصلاح قابلة للتنفيذ.

- [x] تنفيذ التدقيق العميق الأولي للواجهة الأمامية وSupabase والتكامل وحفظ تقرير الأدلة.
- [ ] تقييد EXECUTE لدوال SECURITY DEFINER الحساسة والتحقق من auth.uid وعضوية الشركة داخل كل دالة.
- [ ] إيقاف Mock Supabase الصامت خارج اختبارات الوحدة وإظهار خطأ إعداد واضح.
- [ ] تحويل خدمات إصلاح القيود المالية في purchaseFixes إلى RPC ذرية مع dry-run وaudit trail.
- [ ] إعادة مزامنة migrations وdatabase.types.ts مع مخطط Supabase المنشور وتصنيف الفروقات.
- [ ] توسيع خريطة Realtime وinvalidation لتشمل العمولات والمجالات المالية والمخزنية مع company scoping.
- [ ] إزالة التكرارات والتحويلات غير الآمنة من طبقات Settings وSupabase وكتابة اختبارات تفويض سالبة.

## جولة توحيد الصلاحيات والتنقل (2026-08-15)

- [x] توحيد عقد صلاحيات العمولات تحت بادئة `incentive:*` في `authorization.ts` عبر `COMMISSION_PERMISSIONS` ودوال `canManagePlansByAccess`/`canCalculatePeriodByAccess`/`transitionPermission`، وإزالة الاسم المنحرف `commission:calculate` من لوحة العمولات.
- [x] تحويل `useCommissionDashboardData`/`useCommissionConfigurationData`/`useCommissionPeriodsController` إلى استخدام `authorization.ts` بدل الشيكات المبعثرة؛ وأصبح `manager` دورًا معترفًا (يطابق `user_is_admin_or_manager()` خادميًا).
- [x] إضافة migration `20260815000001_add_commission_permissions.sql` يزرع `incentive:*` في `role_permissions` للأدوار `admin`/`manager` مع `ON CONFLICT DO NOTHING`.
- [x] توسيع نوع `Permission` في `common.ts` والخريطة القديمة في `core/permissions/index.tsx` بصلاحيات `incentive:*`.
- [x] إصلاح التكرارات: `useCommissionReportData` يستخدم `commissionQueryKeys.reportPeriods/reportCalculations`، وإزالة `companyId ?? ''` من تعيينات المهندسين.
- [x] توطين عناوين العمولات في `MainLayout` و`MENU_ITEMS` عبر `t()` + إضافة وحدة العمولات للقائمة الجانبية الرئيسية.
- [x] إصلاح أخطاء `exactOptionalPropertyTypes` القائمة مسبقًا في hooks وحدة العمولات (تغيير `x?` إلى `x: T | undefined`).
- [x] اختبارات Vitest 12/12 ناجحة، وفحص TypeScript مجزأ خالٍ من الأخطاء في ملفات الوحدة، مع بقاء الأخطاء العامة في وحدات أخرى (محاسبة/سندات) خارج نطاق الجولة.
- [x] تحسين تصميم صفحة العمولات للحفاظ على هوية التطبيق البصرية، مع رفع وضوح التسلسل البصري ودعم كثافة الهاتف وRTL.
- [x] التحقق من صفحة العمولات واختبارها ثم رفع تحسينات التصميم إلى GitHub.

- [x] ضغط مقاسات صفحة فاتورة مشتريات جديدة وتقليص المسافات والفراغات بين المكونات على الهاتف فقط.
- [x] إصلاح قائمة الصندوق بحيث تظهر خياراتها عند الضغط وتسمح بالاختيار الصحيح.
- [x] إصلاح قائمة المستودع بحيث تظهر خياراتها عند الضغط وتسمح بالاختيار الصحيح.
- [x] اختبار عدم تأثر سطح المكتب واللوحي ومنطق حفظ الفاتورة بعد الإصلاحات.

- [x] تحجيم صفحة العمولات وضغط الفراغات والمسافات على الهاتف فقط مع إبقاء سطح المكتب واللوحي دون تغيير.
- [x] توحيد عرض الأرقام الإنجليزية في واجهة التطبيق مع الحفاظ على RTL وعدم تغيير القيم المخزنة أو الحسابات.
- [x] إضافة اختبارات للتحقق من عرض الأرقام الإنجليزية واستجابة صفحة العمولات.
- [x] رفع تحديثات صفحة العمولات وتنسيق الأرقام إلى الفرع الرئيسي.

- [x] فحص صفحة فاتورة مبيعات جديدة ومكوناتها ومسارات الحفظ والحسابات والقوائم.
- [x] اختبار صفحة فاتورة المبيعات على الهاتف وسطح المكتب وتحديد الأخطاء المؤكدة.
- [x] إصلاح الأخطاء المؤكدة مع الحفاظ على RTL وعدم تغيير السلوك السليم.
- [ ] إعادة تشغيل الاختبارات وفحص diff ورفع تحديثات فاتورة المبيعات إلى الفرع الرئيسي.

- [x] مراجعة عميقة لمكونات صفحة فاتورة مشتريات جديدة ومسار الحالة والحفظ.
- [x] فحص الحقول والقوائم والحسابات والمخزون والتكامل مع Backend/RPC.
- [x] إصلاح الأخطاء المؤكدة وإضافة اختبارات للحالات الحرجة.
- [ ] تنفيذ فحص نهائي ورفع تحديثات مراجعة فاتورة المشتريات إلى الفرع الرئيسي.
