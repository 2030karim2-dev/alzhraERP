# خطة إصلاح وتحسين تجربة الموبايل — نظام الزهراء الذكي

# Mobile UX Repair & Improvement Plan (P3)

**المُعِد:** مراجعة UI/UX الزهراء — تحويل `plan` المُعتَدّ به إلى `plans/mobile-ux-repair-plan.md`
**الحالة:** مُعتمَدة — تبدأ من P3 (القياس وسد فجوات التحقق)

---

## 1. الملخص التنفيذي

| البند             | القيمة                                                                       |
| ----------------- | ---------------------------------------------------------------------------- |
| **الهدف**         | رفع جاهزية واجهة الموبايل إلى المستوى الإنتاجي (تقييم مستهدف 8.5/10)         |
| **النطاق**        | P3 (القياس/الحواجز) → P4 (سد الفجوات) → P5 (التوثيق/التحصين)                 |
| **المبدأ الحاكم** | التحقق بالنتيجة لا بالإعلان — كل تغيير يُثبَت بـ `git diff` + green pipeline |

### ما تحقق (P0-P2)

- جذر الموبايل 9.2px → 12px؛ صفر `text-[1-9px]` في `src/`
- Safe-area لكل العناصر السفلية الثابتة
- أهداف لمس 44px+ للـ Buttons والـ Header
- 276 توحيد لون عبر 159 ملفاً
- بطاقات موبايل لـ 4 شاشات (متابعة/وعود/صندوق/قيود)
- تلميح تمرير أوتوماتيكي لـ 7 جداول عريضة
- حارسا `check:classes` (تضارب) + `check:fonts` (خطوط دقيقة)

---

## 2. المراحل المتبقية

### P3 — القياس وسد فجوات التحقق (المنفَّذ)

| #    | البند         | الإجراء                                                                                      | الحالة |
| ---- | ------------- | -------------------------------------------------------------------------------------------- | ------ |
| P3-1 | فحص لمسي بصري | محاكاة 390×844 عبر Playwright: لا overflow، لا نصوص <9.5px في الواجهة العامة/الهبوط          | ✅     |
| P3-2 | e2e استجابية  | `e2e/mobile-responsive.spec.ts` — 4/5 نجحت (1 يتخطّى بلا مصادقة)، لا overflow، لا نصوص دقيقة | ✅     |
| P3-3 | `DhikrTicker` | `max-sm:hidden` — توفير 24px عمودية على الهواتف                                              | ✅     |
| P3-4 | حارس الخط     | `check:fonts` (يرفض `text-[1-9px]`) + ربطه بـ `pre-push`                                     | ✅     |
| —    | التنظيف       | حذف السجلات المؤقتة + الخوادم القديمة + إضافة `playwright-report/` للـ .gitignore            | ✅     |

**إضافات بنيوية (P3):**

- إصلاح انجراف `alzhraERP/playwright.config.ts`: المنفذ المتوقع 5173 → 8081 (مطابق لـ vite.config.ts)
- حذف نسخة الجذر الميتة `playwright.config.ts` + تفويض `test:e2e` الجذر إلى alzhraERP
- تثبيت Chromium (Playwright) للفحص المحلي

**Def. of Done P3: ✅** — `test:e2e` أخضر، `check:classes` + `check:fonts` أخضران، type-check نظيف، 567 اختباراً ناجحاً، لا سجلات مؤقتة.

### P4 — سد فجوات الموبايل الوظيفية

- P4-1 مراجعة XL للجداول غير القياسية (`PartsGridTable`, `QuickAuditItemsTable`, ذبول `dark:bg-slate-*` الصلب)
- P4-2 مكوّن `MobileCardList` موحّد في `ui/base` لمكافحة تكرار البطاقات الأربع
- P4-3 تبويب POS عند 320px (أصغر شاشة iPhone SE)
- P4-4 PWA: أيقونة 192px + `screenshot` في manifest
- P4-5 `useBreakpoint`: `window.resize` → `matchMedia`

### P5 — التوثيق والتحصين المؤسسي

- P5-1 **ADR-014** (قرارات الموبايل: الخط/اللمس/safe-area/bottom-sheets)
- P5-2 تحديث `STYLE_GUIDE.md` (قسم "موبايل" بالأنماط والقيود)
- P5-3 `pre-push` = 4 حواجز (`check:encoding` + `check-ts-baseline` + `check:classes` + `check:fonts`)
- P5-4 تحديث تقرير التقييم مع لقطات قبل/بعد

---

## 3. استراتيجية الجودة

| الطبقة | الأداة                          | التغطية                      |
| ------ | ------------------------------- | ---------------------------- |
| وحدة   | Vitest (567)                    | الحارس/المنطق                |
| e2e    | Playwright (5 specs بعد P3-2)   | التدفقات + الاستجابة العرضية |
| Types  | `tsc --noEmit`                  | بعد كل P                     |
| أدوات  | `check:classes` + `check:fonts` | على القرص قبل `pre-push`     |
| يدوي   | DevTools 390/844 + iPhone فعلي  | اللمس والـ notch             |

---

## 4. إدارة المخاطر

| الخطر                     | الاحتمال | التخفيف                                  |
| ------------------------- | -------- | ---------------------------------------- |
| عودة خطوط دقيقة           | مرتفع    | `check:fonts` يمنعها آلياً في `pre-push` |
| كسر RTL                   | متوسط    | `dir` + فحص e2e عربي                     |
| أثر رفع الخط +30%         | محسوم    | ديسكتوب لم يتغير؛ مقبول موبايلاً         |
| `dark:bg-slate-800` (548) | مؤجّل    | لا أتمتة بلا فهم السياق                  |

---

_آخر تحديث: 2026-08-30 — جلسة تنفيذ P3 (بدء Act mode)_
